extends base
include mixins/video

block content
    form(method="GET")
        input(name="keyword", placeholder="searchByTitle", type="text", required)
        input(type="submit" value="Search")
    
    each video in videos
        +video(video)
    